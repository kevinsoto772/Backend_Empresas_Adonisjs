/api/v1/maestra/cambiarEstado/{id}: 
  put:
    tags:
      - Maestra
    description: Cambiar el estado de una Tabla Maestra
    parameters:
      - name: id
        in: path
        type: string
        required: true
        description: El id de la Tabla Maestra a la cual se le quiere cambiar el estado
    produces:
      - application/json
    responses:
      200:
        description: Cambio realizado correctamente
        content:
          application/json:
            schema:
              type: object
              properties:
            example: "Cambio realizado correctamente"