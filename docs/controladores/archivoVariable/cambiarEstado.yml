/api/v1/archivo_variable/cambiarEstado/{id}: 
  put:
    tags:
      - ArchivoVariable
    description: Cambiar el estado del archivo variable
    parameters:
      - name: id
        in: path
        type: string
        required: true
        description: El id del archivo variable al cual se le quiere cambiar el estado
    produces:
      - application/json
    responses:
      200:
        description: Cambio realizado correctamente
        content:
          application/json:
            schema:
              type: object
              properties:
            example: "Cambio realizado correctamente"