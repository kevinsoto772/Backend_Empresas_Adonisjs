/api/v1/variable_especifica/cambiarEstado/{id}: 
  put:
    tags:
      - VariableEspecifica
    description: Cambiar el estado de una variable especifica
    parameters:
      - name: id
        in: path
        type: string
        required: true
        description: El id de la variable especifica a la cual se le quiere cambiar el estado
    produces:
      - application/json
    responses:
      200:
        description: Cambio realizado correctamente
        content:
          application/json:
            schema:
              type: object
              properties:
            example: "Cambio realizado correctamente"