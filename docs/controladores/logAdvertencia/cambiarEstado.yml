/api/v1/logsAdvertencias/cambiarEstado/{id}: 
  put:
    tags:
      - LogsAdvertencias
    description: Cambiar el estado de una advertencia
    parameters:
      - name: id
        in: path
        type: string
        required: true
        description: El id de la advertencia a la cual se le quiere cambiar el estado
    produces:
      - application/json
    responses:
      200:
        description: Cambio realizado correctamente
        content:
          application/json:
            schema:
              type: object
              properties:
            example: "Cambio realizado correctamente"