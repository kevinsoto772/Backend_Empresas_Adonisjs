/api/v1/logsErrores/cambiarEstado/{id}: 
  put:
    tags:
      - LogErrores
    description: Cambiar el estado del error
    parameters:
      - name: id
        in: path
        type: string
        required: true
        description: El id del error al cual se le quiere cambiar el estado
    produces:
      - application/json
    responses:
      200:
        description: Cambio realizado correctamente
        content:
          application/json:
            schema:
              type: object
              properties:
            example: "Cambio realizado correctamente"