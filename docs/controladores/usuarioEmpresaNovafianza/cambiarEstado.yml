/api/v1/usuario_novafianza/cambiarEstado/{id}: 
  put:
    tags:
      - usuarioNovafianza
    description: Cambiar el estado del usuario de Novafianza
    parameters:
      - name: id
        in: path
        type: string
        required: true
        description: El id del usuario al cual se le quiere cambiar el estado
    produces:
      - application/json
    responses:
      200:
        description: Cambio realizado correctamente
        content:
          application/json:
            schema:
              type: object
              properties:
            example: "Cambio realizado correctamente"