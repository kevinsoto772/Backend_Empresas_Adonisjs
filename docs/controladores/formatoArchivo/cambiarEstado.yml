/api/v1/formato_archivo/cambiarEstado/{id}: 
  put:
    tags:
      - FormatoArchivo
    description: Cambiar el estado de formato de archivo
    parameters:
      - name: id
        in: path
        type: string
        required: true
        description: El id de un formato de archivo al cual se le quiere cambiar el estado
    produces:
      - application/json
    responses:
      200:
        description: Cambio realizado correctamente
        content:
          application/json:
            schema:
              type: object
              properties:
            example: "Cambio realizado correctamente"