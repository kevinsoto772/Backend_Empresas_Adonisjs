/api/v1/logsExitos/cambiarEstado/{id}: 
  put:
    tags:
      - LogExito
    description: Cambiar el estado de un exito
    parameters:
      - name: id
        in: path
        type: string
        required: true
        description: El id del exito al cual se le quiere cambiar el estado
    produces:
      - application/json
    responses:
      200:
        description: Cambio realizado correctamente
        content:
          application/json:
            schema:
              type: object
              properties:
            example: "Cambio realizado correctamente"