/api/v1/variable_transversal/cambiarEstado/{id}: 
  put:
    tags:
      - VariableTransversal
    description: Cambiar el estado de una variable transversal
    parameters:
      - name: id
        in: path
        type: string
        required: true
        description: El id de la variable transversal a la cual se le quiere cambiar el estado
    produces:
      - application/json
    responses:
      200:
        description: Cambio realizado correctamente
        content:
          application/json:
            schema:
              type: object
              properties:
            example: "Cambio realizado correctamente"