/api/v1/archivo_empresa_formato/cambiarEstado/{id}: 
  put:
    tags:
      - ArchivoEmpresaFormato
    description: Cambiar el estado de un formato de archivo de una empresa
    parameters:
      - name: id
        in: path
        type: string
        required: true
        description: El id del formato de archivo de una empresa al cual se le quiere cambiar el estado
    produces:
      - application/json
    responses:
      200:
        description: Cambio realizado correctamente
        content:
          application/json:
            schema:
              type: object
              properties:
            example: "Cambio realizado correctamente"